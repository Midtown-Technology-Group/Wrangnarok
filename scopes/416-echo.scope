# ADR-033-5 echo migration (issue #416, 3/6): echo onto the ADR 033
# interior helpers with byte-identical behavior; first timeout-mark-v1
# removal (failSagaExecution classifies inside persist-failure-v1). Pins
# the migrated TimedOut path in the terminal + run-path suites and makes
# the legacy-step comments timeless. No other Saga, no contract change,
# no new primitive. Worker+D1 only.
scopes/416-echo.scope
src/sagas/echo.ts
src/domain.ts
test/terminal-outcomes.test.ts
test/saga-run-paths.test.ts
