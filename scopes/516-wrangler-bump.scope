# Wrangler 4.131.2 bump stabilization lane (PR #516): take the dependabot
# wrangler 4.131.1 -> 4.131.2 bump (package.json + lockfile, transitive
# miniflare dedup only; workerd unchanged) and add degraded-runner headroom
# for the workerd suite after the bump run showed scattered 5s/120s timeouts
# on a ~3x-slowed shared runner (357s cumulative on main vs 1195s on the bump
# run, failing files varying run to run). Test config only: workers-project
# testTimeout/hookTimeout plus the two explicit schedule-tick fairness
# budgets. No new Cloudflare primitive, no DSL, no execution semantics change.
scopes/516-wrangler-bump.scope
package.json
package-lock.json
vitest.config.ts
test/schedule-tick.test.ts
