# AI-01 secret-scrub lane scope (issue #164): dependency-ready gap only.
# Registers the five existing deployment-global AI provider API keys in the
# one authoritative secret registry (src/secrets.ts) plus sentinel
# regressions. Preserves ADR 032 v0: no per-tenant keys, no D1 persistence,
# no provider HTTP, no migration, no new primitive, no profile CRUD or
# inference work.
scopes/ai-164-secret-scrub.scope
src/secrets.ts
docs/upstream-parity.md
test/secret-scrub.test.ts
