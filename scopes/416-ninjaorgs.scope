# ADR-033-5 ninjaorgs migration (issue #416, 4/6): ninjaone-orgs onto the
# ADR 033 interior helpers with byte-identical behavior; timeout-mark-v1
# removed (failSagaExecution classifies inside persist-failure-v1). Pins
# the migrated TimedOut path in the ninjaone suite. No other Saga, no
# contract change, no new primitive. Worker+D1 only.
scopes/416-ninjaorgs.scope
src/sagas/ninjaorgs.ts
test/ninjaone.test.ts
